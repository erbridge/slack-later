{
  "name": "slack-later",
  "private": true,
  "scripts": {
    "start": "npm run build && TZ=UTC node -r dotenv/config build/index.js",
    "build": "tsc",
    "lint": "prettier --check '**/*'",
    "lint:fix": "prettier --write '**/*'"
  },
  "dependencies": {
    "@slack/bolt": "^2.3.0",
    "chrono-node": "^2.1.8",
    "dayjs": "^1.10.3",
    "dotenv": "^8.2.0",
    "typescript": "^4.0.2"
  },
  "devDependencies": {
    "prettier": "^2.1.1"
  }
}
