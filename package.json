{
  "name": "slack-later",
  "private": true,
  "scripts": {
    "start": "npm run build && TZ=UTC node -r dotenv/config build/index.js",
    "build": "tsc",
    "lint": "prettier --check '**/*'",
    "lint:fix": "prettier --write '**/*'"
  },
  "dependencies": {
    "@slack/bolt": "^3.4.1",
    "chrono-node": "^2.2.5",
    "dayjs": "^1.10.4",
    "dotenv": "^8.2.0",
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
